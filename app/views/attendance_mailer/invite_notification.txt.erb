<%= t("email.generic.greeting") %>

<%= t("email.attendance.invite.invite_text", :inviter_name => attendance.event.host.name, :event_name => attendance.event.name) %>

<%= event_url(attendance.event) %>
